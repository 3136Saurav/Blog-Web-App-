<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="stylesheets/favicon.png" type="image/png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    <link rel="stylesheet" href="/stylesheets/app.css">
    <title>Blog App</title>
</head>
<body>
    <div class="ui fixed inverted menu">
        <div class="ui container">
            <div class="header item"><a id="item1" href="/blogs"><i class="pencil alternate icon"></i>Blog Site</a></div>
            <a id="item2" href="/blogs" class="item">Home</a>
            <% if(!currentUser) { %>    
                <a href="/register" class="item">Sign Up</a>
                <a href="/login" class="item">Login</a>
            <% } else { %>
                <a id="item3" href="/blogs/new" class="item">New Post</a>
                <a href="#" class="item">Signed in as <%= currentUser.username %></a>
                <a id="item4" href="/logout" class="item">Logout</a>
            <% } %>
        </div>
    </div>